json.extract! objective, :id, :title, :reason, :people_involved, :resources, :expected_results, :achieved, :metrics, :max_deadline, :min_deadline, :student_id, :created_at, :updated_at
json.url objective_url(objective, format: :json)
