<p style="color: green"><%= notice %></p>

<h1><%= t('goal.index.title') %>:</h1>
<hr>

<div id="goals">
  <% @goals.each do |goal| %>
    <%= render goal %>
    <p>
      <%= link_to t("goal.index.show_this_goal"), goal %>
    </p>
    <hr>
  <% end %>
</div>

<%= link_to t("goal.index.new_goal"), new_goal_path %>
