<%= form_with(model: thought) do |form| %>
  <% if thought.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(thought.errors.count, "error") %> prohibited this thought from being saved:</h2>

      <ul>
        <% thought.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :level, t('views.thoughts.form.level_label') %>:
    <%= form.select :level, Thought::LEVEL_OPTIONS %>
  </div>

  <div>
    <%= form.label :type_thought, t('views.thoughts.form.type_thought_label')  %>:
    <%= form.select :type_thought, Thought::LEVEL_OPTIONS %>
  </div>

  <div>
    <%= form.label :description, t('views.thoughts.form.description_label')  %>:
    <%= form.text_area :description %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
