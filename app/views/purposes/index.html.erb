<p style="color: green"><%= notice %></p>

<h1><%= t('views.purpose.index.title') %></h1>

<div id="purposes">
  <% @purposes.each do |purpose| %>
    <%= render purpose %>
    <p>
      <%= link_to t('views.purpose.index.show_link'), purpose %>
    </p>
  <% end %>
</div>

<%= link_to t('views.purpose.index.new_button'), new_purpose_path %>
